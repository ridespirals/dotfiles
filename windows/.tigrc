bind generic ; prompt

bind generic P parent

bind generic T !@sh -c "git show -s --format=%H %(commit) | tr -d '\r\n' | pbcopy"

bind stash D !?git stash drop %(stash)
bind stash A !?git stash apply %(stash)

