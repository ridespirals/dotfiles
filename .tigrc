bind generic ; prompt

bind generic P parent

# copy selected commit hash
bind generic T !@sh -c "git show -s --format=%H %(commit) | pbcopy"

# Stashes
bind stash D !?git stash drop %(stash)
bind stash A !?git stash apply %(stash)

color cursor white red bold
